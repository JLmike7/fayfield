# Usable Original event URLs — 2026-09-12

Michael FAIL: relative Court URLs broke on github.io.

**Fix:** `usableEventUrl` at snapshot + UI + catalog map — resolve relative against homepage; keep only absolute http(s); drop CivicEngage iCalendar feed URLs. No link > broken link.
