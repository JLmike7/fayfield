# Filters label wrap beside checkbox — 2026-09-12

Michael FAIL: source names wrapped under checkbox on mobile.

Fix: `.cal-source-name` span + label `flex-wrap: nowrap`; name `flex:1; min-width:0` wraps beside the box.
