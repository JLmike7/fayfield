# Sitewide centered shell — 2026-09-13

`--shell-max: 40rem` (calendar `48rem` via `body:has(.cal-controls)`). header/main/footer `margin-inline: auto`. Useful Links jump-band + hrs full-bleed via `100vw` + `margin-left: calc(50% - 50vw)`. Mobile unchanged (shell wider than viewport).
